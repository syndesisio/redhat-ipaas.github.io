machine:
  node:
    version: 7.4.0
  ruby:
    version: 2.4.0

general:
  branches:
    ignore:
    - gh-pages

dependencies:
  pre:
  - gem install bundler

checkout:
  post:
    - bundle install
    - bash ./scripts/deploy-ghpages.sh
